import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Edit Policy',
  description: 'Edit your access policy',
}

const Layout = ({ children }: { children: React.ReactNode }) => {
  return children
}

export default Layout
