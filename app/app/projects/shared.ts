// Include configuration for project shared between layout and page
export const projectIncludeConfig = {
  users: { user: true },
  tasks: { assignedTo: true, tags: true },
  taskCategories: true,
}
